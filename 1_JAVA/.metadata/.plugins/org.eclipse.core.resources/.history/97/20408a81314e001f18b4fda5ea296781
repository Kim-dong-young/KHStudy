package p.network;

public class TCPServer {
	/*
	 * TCP
	 * - 서버, 클라이언트 간에 1:1 소켓 통신
	 * - 데이터를 교환하기에 앞서 서버, 클라이언트 연결이 되어야 한다.
	 *   (서버가 먼저 실행되에 클라이언트의 요청을 기다린다.)
	 * - 신뢰성있는 데이터 전달 가능
	 * 
	 * socket
	 * - 프로세스간의 통신을 담당
	 * - input/outputStream을 가지고 있다.
	 * 
	 */
	public static void main(String[] args) {
		
	}
}
